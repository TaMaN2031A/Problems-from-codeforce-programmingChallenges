#include <stdio.h>
#include <stdlib.h>
#define max 100
int main()
{
    int n,arr[max],k = 0, mid = 0, end;
    scanf("%i %i",&n, &k);
    mo
    for(int i = 0; i < n; i++)
    {
        scanf("%i", &arr[i]);

    }
    // assume we sorted it
    while(tryn == 1)
    {
        if(a[mid]*k > a[n-1])
        {
            tryn = 0;
            end = mid;
        }
        else
        {
            mid =(mid+end)/2;
        }
    }
    tryn = 1;
    while(tryn == 1)
    {
        if(a[mid]/k < a[0])
        {
            tryn = 0;
            start = mid;
        }
        else
        {
            mid =(mid+start)/2;
        }
    }
        return 0;
}
